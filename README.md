# KubeSling Demo: Mistral-7B on GPU

Health: GET /health
Infer: POST /infer { "prompt": "...", "max_new_tokens": 40 }

MODEL_ID default: mistralai/Mistral-7B-Instruct (override via env var if needed)
